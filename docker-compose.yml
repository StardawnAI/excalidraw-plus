services:
  excalidraw-plus:
    image: ghcr.io/stardawnai/excalidraw-plus:latest
    ports:
      - "3010:3000"
    env_file:
      - .env  # Optional: for server-side LLM config
    environment:
      - NODE_ENV=production
    restart: unless-stopped
